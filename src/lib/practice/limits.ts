// Central practice limits are kept outside the route module because Next.js
// route files may export only HTTP handlers and supported route metadata.
export const MAX_REP_TURNS = 30;

export const MAX_TURN_TEXT_LENGTH = 4000;
